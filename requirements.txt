# Core RAG + UI stack — runs on a light cloud server (no torch / no GPU needed).
streamlit>=1.40.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0          # OpenAI + DeepSeek + Kimi/Moonshot + HF router (OpenAI-compatible)
langchain-google-genai>=2.0.0    # Gemini chat + embeddings
langchain-huggingface>=0.1.0     # Hugging Face inference (chat + endpoint embeddings)
google-generativeai>=0.8.0
huggingface_hub>=0.25.0
faiss-cpu>=1.8.0                  # local vector store, no server
rank_bm25>=0.2.2                  # lexical BM25 for hybrid retrieval
pypdf>=5.0.0
pdfplumber>=0.11.0               # extract financial tables (linearized for retrieval)
python-dotenv>=1.0.0
tiktoken>=0.8.0
pandas>=2.0.0                     # eval comparison tables
PyYAML>=6.0
